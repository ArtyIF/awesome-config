SMALL_ELEMENTS = false
PERFORMANCE_MODE = false
LIGHT_THEME = false
SMART_TITLEBAR_COLOR = false -- false (never recolor the titlebar), "static" (recolor once with a 0.5s delay) or "dynamic" (recolor every 0.5s, uses lots of cpu for gc!)